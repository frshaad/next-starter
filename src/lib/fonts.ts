import { Inter, Vazirmatn } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const vazir = Vazirmatn({
  subsets: ['arabic'],
  weight: ['400', '500', '700'],
  display: 'swap',
});
